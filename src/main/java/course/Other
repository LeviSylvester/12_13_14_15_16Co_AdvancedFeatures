mvn clean install

file settings build compiler java target 8

sa punem testul in pachetul cu acelasi nume ca si in src.java

create java doc for methods

EQUALS CONTRACT (if you want to change ex. remove method, implicit metoda equals)
-tranzitivitate:
x equals y
y equals z
=> x equals z
-consistenta:
x equals y
y equals z
-identitate:
x equals x

daca putem, o metoda facem statica, este verificat o singura data la compilare, nu la fiecare instanta

hip memory - memory that java works with (exception if overflow with generated objects, or ex. overflow int)