package course.composition;

public class Composition {
    //"HAS-A" relation

    //ex. package shape class point (aggregation)

    //compostiion vs aggregation = if content does(a) or does not (c) make sense without the container


    //static/final
}
