package shapes;

public class App {
    public static void main(String[] args) {
//        Circle circle = new Circle(2.);Shape genericShape = new Shape(0);


        Shape.Color color = new Shape.Color(34, 186, 284); //nested static class
        System.out.println(color);

//        Circle.InnerPoint innerpoint = circle.new InnerPoint(...); //a instantia o clasa inner class
    }
}
